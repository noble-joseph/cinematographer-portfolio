# Strapi Backend Configuration
REACT_APP_STRAPI_URL=http://localhost:1337

# Optional: Strapi API Token (only needed if public permissions not working)
# Get from Strapi Admin: Settings > API Tokens > Create new API Token
# REACT_APP_STRAPI_TOKEN=your_token_here
